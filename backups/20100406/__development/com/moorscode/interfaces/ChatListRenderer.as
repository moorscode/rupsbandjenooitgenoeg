package com.moorscode.interfaces {	import fl.controls.listClasses.ICellRenderer;	import fl.controls.listClasses.ImageCell;	import fl.controls.TileList;	import flash.text.*;		public class ChatListRenderer extends ImageCell implements ICellRenderer {  		private var username:TextField;		private var textStyle:TextFormat;		    	public function ChatListRenderer() {			super();						loader.scaleContent = true;						// Create and format desc			username = new TextField();			username.autoSize = TextFieldAutoSize.LEFT;			username.x = 30;			username.y = 10;//			username.width = 30;			username.multiline = false;			username.wordWrap = false;			addChild(username);						textStyle = new TextFormat();			textStyle.font = "Tahoma";			textStyle.color = 0x000000;			textStyle.size = 11;    	}			override protected function drawLayout():void {			// Position cell elements; tweak these for your thumbs if needed			var imagePadding:Number = getStyleValue("imagePadding") as Number;			loader.move(5, 2);						loader.setSize(25, 25);			loader.drawNow();						username.text = data.label;			username.setTextFormat(textStyle);						background.width = width + 5;			background.height = height + 1;						textField.width = 1;			textField.visible = false;		}	}}